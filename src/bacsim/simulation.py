"""The class 'Simulation' and its helper functions."""

def run_simulation(input_filename, output_filename):
    """Run a simulation."""
    assert isinstance(input_filename, str)
    assert isinstance(output_filename, str)
    pass